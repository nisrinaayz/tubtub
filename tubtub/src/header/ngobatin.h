#include "obat.h"

void F12_ngobatin(RuanganList *ruanganList, UserSet *userSet,
                  PenyakitObatMap *peta, int jumlahPenyakit,
                  InventoryObat *inventorySet, Session session);